#
# Copyright (c) 2022 Wind River Systems, Inc.
#
# SPDX-License-Identifier: Apache-2.0
#

apiVersion: "helm.toolkit.fluxcd.io/v2beta1"
kind: HelmRelease
metadata:
  name: psp-rolebinding
  labels:
    chart_group: cert-manager
spec:
  releaseName: cm-cert-manager-psp-rolebinding
  chart:
    spec:
      chart: psp-rolebinding
      version: 0.1.0
      sourceRef:
        kind: HelmRepository
        name: stx-platform
  interval: 5m
  timeout: 30m
  test:
    enable: false
  install:
    disableHooks: false
  upgrade:
    disableHooks: false
    force: true
  valuesFrom:
    - kind: Secret
      name: cert-manager-psp-rolebinding-static-overrides
      valuesKey: cert-manager-psp-rolebinding-static-overrides.yaml
    - kind: Secret
      name: cert-manager-psp-rolebinding-system-overrides
      valuesKey: cert-manager-psp-rolebinding-system-overrides.yaml
